setIcon(<quark:chain>)

setTitle("Chain")

setDescription("Craft a Chain. Used to connect minecarts!")

addParent("redstone:pressure_detector")

addRewardFunction("redstone:chain")

addRewardFunction("experience:common")

criteria = addCriteria("chain", "minecraft:inventory_changed")

criteria.addItem(<quark:chain>)